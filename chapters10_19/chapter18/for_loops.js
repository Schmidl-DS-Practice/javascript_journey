var cleanest_cities = ["cheyenne", "santa fe", "tucson", "great falls", "honolulu"];


// variable that counts iterations, and also serves as the array index, is declared and set to a starting value
// limit on the loop
// what happens at the end of every loop
for (var i = 0; i <= 4; i++) {
    if (cityToCheck === cleanest_cities[i]){
        alert("it's one of the cleanest cities");}}